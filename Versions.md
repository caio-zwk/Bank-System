# Changelog

## v0.2 - 2026-08-14
- Aplicadas correções básicas:
  - Validação de entradas (senha, menu, valores) para evitar crashes.
  - Limite de 3 tentativas para login.
  - Formatação do saldo com `NumberFormat` (pt-BR).
  - Correções de strings/typos.
  - Adicionada opção no menu para criar nova conta.
  - `ContaBanco` movida para `ContaBanco.java` com métodos `depositar()` e `sacar()`.

## v0.1 - Inicial
- Projeto criado com `Main.java` e contas de exemplo embutidas.
